const multiply = function (a, b) {
  //anonymous function which don't have any function name after function keyword declared, it defined in a variable
  return a * b;
};

const res = multiply(4, 4);
console.log(res);


const add = (a,b) => a+b;
const res2 = add(2,8);
console.log(res2);